const superHeros: string[] = [];
// const heroPower: number[] = [];
const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

superHeros.push("spiderman");
heroPower.push(2);

const MLModels: number[][] = [
  [255, 255, 255],
  [255, 255, 255],
  [255, 255, 255],
];

allUsers.push({ name: "str", isActive: true }, { name: "str", isActive: true });
